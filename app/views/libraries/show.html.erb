

<script type="text/javascript">
  function initialize() {

    var myLatlng = new google.maps.LatLng(49.20861111111111, 16.593888888888888);
    var mapOptions = {
      zoom: 10,
      center: myLatlng
    }
    var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);


    var contentString = '<div id="content">'+
      '<div id="siteNotice">'+
      '</div>'+
      '<h2 id="firstHeading" class="firstHeading"><%= @library.name%></h1>'+
      '<div id="bodyContent">'+
      '<p>' +
      '<%= @library.street %>' + '<br/>' +
      '<%= @library.city %>' + '<br/>' +
      '<%= @library.zip %>' + '<br/>' +
      '</p>'+
      '</div>'+
      '</div>';

    var infowindow = new google.maps.InfoWindow({
      content: contentString
    });


    var marker = new google.maps.Marker({
      position: myLatlng,
      map: map,
      title: '<%= @library.name%>'
    });


    google.maps.event.addListener(marker, 'click', function() {
      infowindow.open(map,marker);
    });


  }
  google.maps.event.addDomListener(window, 'load', initialize);
</script>




<div class="cl-library-detail">
<h2><%= @library.name %></h2>

<div class="row">
  <div class="col-md-8">

  <div id="map-canvas" style="width: 100%; height: 400px;"></div>

    <br/>
    <p><%= @library.description %></p>    
  </div>

  <div class="col-md-4">
   <div class="cl-right">
    <p><%= @library.street %></p>
    <p><%= @library.city %></p>
    <p><%= @library.zip %></p>
    <br/>
    <p>Web: <a href="<%= @library.url %>"><%= @library.url %></a></p>
    <p>Tel.: <%= @library.phone %></p>
    <p>Email: <%= @library.email %></p>
    <br/>
    <p><%= @library.context %></p>
    <br/>
    <p>Kraj: <%= @library.district %></p>
    <p>Město: <%= @library.town %></p>
    <br/>
    <p>Sigla: <%= @library.sigla %></p>    
   </div> 
  </div>
</div>

<%= link_to 'Upravit', edit_library_path(@library) %>
</div>