<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, t('events.new.title') %><br>
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :description, t('events.new.description') %><br>
    <%= f.text_area :description, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :url, t('events.new.url') %><br>
    <%= f.text_field :url, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :place, t('events.new.place') %><br>
    <%= f.text_field :place, class: 'form-control' %>
  </div>  
  <div class="field">
    <%= f.label :date, t('events.new.date') %><br>
    <%= f.text_field :date, class: 'form-control datepicker' %>
  </div>
  <div class="actions">
    <%= f.submit t('common.save'), class: "btn-primary" %>
  </div>
<% end %>

